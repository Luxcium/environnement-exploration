{
  "name": "environnement-exploration",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "setup": "./scripts/setup.sh",
    "deploy": "./scripts/deploy.sh",
    "create-fullstack-app": "npx create-next-app@latest my-fullstack-app --ts --tailwind --src-dir --app --import-alias '@/*' --use-bun --turbo"
  }
}